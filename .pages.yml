media: media
content:
  - name: pages
    label: Pages
    type: collection
    path: docs
    fields:
      - name: title
        type: string
      - name: Anforderung
        type: string
      - name: Process
        type: string
      - name: body
        type: rich-text
      - name: status
        type: select
        options:
          values: [Draft, Review, Published]
